#!/usr/bin/env bash
# claude コンテナのエントリポイント。claude remote-control は複数リポジトリを
# 扱えず(1プロセス=1ディレクトリに固定)このpodの用途に合わないため使わない。
# claude 本体への手動アクセスは sshd コンテナ(kubectl exec / SSH ログイン)から
# 行う前提で、このコンテナはただ待機するだけにする。
set -euo pipefail
exec sleep infinity
