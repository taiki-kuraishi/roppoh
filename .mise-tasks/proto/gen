#!/usr/bin/env bash
#MISE description="Regenerate discord-events-schemas from proto (buf build -> gen.ts)"

set -euo pipefail

# buf.yaml lives at the repo root; mise tasks run from config_root.
buf build -o packages/discord-events-schemas/descriptorset.binpb
bun run packages/discord-events-schemas/src/gen.ts
