# ⚠️ TEMPLATE — `htpasswd -nbB ci '<password>'` の出力を htpasswd に入れ、
#    `sops -e -i k8s/secrets/zot/zot-htpasswd.sops.yaml` で暗号化してからコミットすること。
apiVersion: v1
kind: Secret
metadata:
  name: zot-htpasswd
  namespace: zot
type: Opaque
stringData:
  htpasswd: "ci:$2y$05$REPLACE_ME_BCRYPT_HASH"
