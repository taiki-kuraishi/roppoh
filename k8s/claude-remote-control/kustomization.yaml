apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: claude-remote-control

resources:
  - namespace.yaml
  - service.yaml
  - statefulset.yaml

configMapGenerator:
  - name: claude-remote-control-authorized-keys
    files:
      - files/authorized_keys
