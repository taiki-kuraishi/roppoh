apiVersion: v1
kind: Service
metadata:
  name: llama-cpp-turboquant
  namespace: llama-cpp
  labels:
    app.kubernetes.io/name: llama-cpp-turboquant
    app.kubernetes.io/managed-by: argocd
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: llama-cpp-turboquant
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
