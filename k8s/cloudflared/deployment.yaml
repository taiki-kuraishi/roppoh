apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflared
  namespace: cloudflared
  labels:
    app.kubernetes.io/name: cloudflared
    app.kubernetes.io/managed-by: argocd
spec:
  # 2 レプリカで HA 構成（同一 Tunnel に複数 connector が接続できる Cloudflare の仕様を利用）
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: cloudflared
  template:
    metadata:
      labels:
        app.kubernetes.io/name: cloudflared
    spec:
      # n100/z390 どちらにも配置できるようにし、podAntiAffinity で
      # 2レプリカを別ノードへ散らして冗長性を確保する(ノード指定はしない)。
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app.kubernetes.io/name: cloudflared
                topologyKey: kubernetes.io/hostname
      securityContext:
        runAsNonRoot: true
        # cloudflare/cloudflared の公式 non-root ユーザー
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: cloudflared
          # 【自己更新】このイメージタグは Renovate が更新する
          image: cloudflare/cloudflared:2026.7.2
          args:
            - tunnel
            - --no-autoupdate
            - --loglevel
            - info
            - --metrics
            - 0.0.0.0:2000
            - run
          env:
            - name: TUNNEL_TOKEN
              valueFrom:
                secretKeyRef:
                  # kubectl create secret generic cloudflared-tunnel-token \
                  #   --namespace cloudflared \
                  #   --from-literal=token=<TUNNEL_TOKEN>
                  name: cloudflared-tunnel-token
                  key: token
          ports:
            - name: metrics
              containerPort: 2000
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /ready
              port: metrics
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /ready
              port: metrics
            initialDelaySeconds: 5
            periodSeconds: 10
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          # 実測(Grafana Mimir, 過去7日): CPU 最大 34m / メモリ最大 32Mi
          resources:
            requests:
              cpu: 40m
              memory: 48Mi
            limits:
              cpu: 100m
              memory: 128Mi
