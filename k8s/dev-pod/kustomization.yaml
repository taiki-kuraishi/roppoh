apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dev-pod

resources:
  - namespace.yaml
  - service.yaml
  - statefulset.yaml

configMapGenerator:
  - name: dev-pod-authorized-keys
    files:
      - files/authorized_keys
